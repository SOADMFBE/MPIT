/* document.onclick = function(event) {
   if (document.querySelector('.none')) {
        document.querySelector('.none').classList.replace('none', 'active');
        console.log(document.querySelector('.active'));
    }
    else 
    document.querySelector('.active').classList.replace('active', 'none');
}

 */